{
  "name": "hassad-hr",
  "version": "1.0.0",
  "private": true,
  "description": "Hassad Coffee Roasters HR app — single-file deployable HTML app",
  "scripts": {
    "build": "tailwindcss -i ./tailwind.input.css -o ./tailwind.css --minify",
    "watch": "tailwindcss -i ./tailwind.input.css -o ./tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
